# GitHub personal access token (fine-grained recommended)
# Create one at: Settings > Developer settings > Personal access tokens > Fine-grained tokens
# Required permissions: Public Repositories (read-only) is sufficient
GH_TOKEN=ghp_your_token_here
